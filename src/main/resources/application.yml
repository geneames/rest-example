spring:
  profiles.active: local-datastore,interleave-shuffle

---

spring:
  profiles: local-datastore

  datasource:
    url: jdbc:hsqldb:mem:decks
    username: SA
    test-on-borrow: true
    test-while-idle: true
    validation-query: select 1;
    max-active: 5

  jpa:
    database: hsql
    show-sql: false
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.HSQLDialect

---
spring:
  profiles: remote-datastore

  datasource:
    url: jdbc:mysql://sema.ckrql5p5mo0t.us-west-2.rds.amazonaws.com:3306/deck
    username: semaAdmin
    password: F17ZL[gl
    test-on-borrow: true
    test-while-idle: true
    validation-query: select 1;
    max-active: 2

  jpa:
    database: mysql
    show-sql: true
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        create-drop: true
---
